# مراجعة ربط الأقسام الجديدة بقاعدة البيانات

لم يُنفذ أي SQL أو تعديل Supabase أو RLS.

فحص `public.categories` للقراءة فقط وجد قسم الملابس الحقيقي:
`id = clothing`, `slug = clothing`. سيُستخدم كما هو، وفروع الملابس تحفظ في
حقل `services.profession` الموجود: ملابس نسائية / ملابس أطفال / ملابس رجالية.
لا يلزم جدول فروع أو عمود جديد.

الأقسام التالية موجودة في تعريف الدليل، لكن لا يوجد لها سجل مناسب في قاعدة
البيانات التي جرى فحصها. أقل إضافة لازمة لتفعيل حفظ الخدمات فيها هي أربعة
سجلات في جدول `categories` الحالي، بعد الموافقة:

| id مقترح جديد | slug الدليل الموجود | name_ar | icon | color |
| --- | --- | --- | --- | --- |
| steel | steel | الحديد والصلب | Factory | blue |
| aluminum-glass | aluminum-glass | الألمنيوم | PanelsTopLeft | blue |
| pvc | pvc | PVC | DoorOpen | blue |
| perfumes-cosmetics | perfumes-cosmetics | كوزمتك ومستحضرات التجميل | SprayCan | pink |

قبل إضافة السجلات يجب إعادة فحص عدم وجود تطابق في id أو slug أو الاسم،
واستخدام أي سجل مناسب أُضيف منذ الفحص بدلاً من تكراره. لا تعدّل سجلاً قديماً.

نموذج إضافة الخدمة الحالي يستبعد الأقسام بلا `dbId`، لأن
`services.category_id` يشترط مرجعاً حقيقياً إلى `categories.id`.
لم يُلغَ هذا الشرط، ولم تُربط الأقسام بقسم عام أو بمعرّفات مخترعة.
لذلك دعم الحفظ للأقسام الأربعة معلق على اعتماد وإضافة السجلات، بينما بيانات
الدليل والأيقونات والبحث وإعدادات الحقول جاهزة في الكود.

أسماء الأقسام القديمة «الألمنيوم والزجاج» و«العطور ومستحضرات التجميل» باقية
في الدليل، وأضيفت الأسماء المطلوبة كبيانات ومرادفات بحث دون قسم عرض مكرر.
