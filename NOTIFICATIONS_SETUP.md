# تفعيل إشعارات الإدارة

القائمة الرئيسية تعرض «الإشعارات» مرة واحدة مع الجرس والعدّاد، وبالأبعاد السابقة نفسها. نافذة المستخدم تعرض إشعارات الإدارة المنشورة فقط، دون أدوات إنشاء أو إرسال أو حذف.

النظام السابق كان يجمع الخدمات والتعليقات في نافذة الإشعارات؛ لم يكن يتضمن رسائل إدارية أو سجل قراءة. لذلك أُضيف جدول مستقل، ولم تُعدّل سياسات الجداول الحالية.

## متطلبات التفعيل

1. نفّذ `supabase_add_admin_notifications.sql` مرة واحدة في SQL Editor للمشروع. الملف لم يُطبّق تلقائيًا على قاعدة البيانات الفعلية.
2. يجب أن تكون دالة `public.is_admin()` الحالية موجودة، وأن يوجد حساب Supabase Auth معتمد كإداري عبر نظام الأدوار الحالي. لا تنفّذ ملفات الترحيل القديمة كاملة لهذا الغرض.
3. افتح لوحة الإدارة ثم «إدارة الإشعارات»، وسجّل الدخول بحساب الإدارة. رمز PIN الذي يفتح اللوحة لا يمنح صلاحية إرسال الإشعارات؛ الصلاحية تُفحص في قاعدة البيانات.
4. أنشئ الإشعار واحفظه كمسودة أو أرسله للجميع. يمكن إرسال المسودات وحذف الإشعارات من هذا القسم فقط.

الإرسال هنا يعني نشر الإشعار داخل التطبيق، وليس إشعار Push من نظام الهاتف.

## عدّاد القراءة

- تُحفظ القراءة محليًا على الجهاز لأن المستخدم العادي لا يملك تسجيل دخول في التطبيق الحالي.
- تُحسب الإشعارات المنشورة غير المقروءة فقط؛ تُستبعد المسودات والمحذوفات.
- تظهر الشارة عند وجود غير مقروء، وتعرض `99+` للأعداد الكبيرة مع العدد الكامل في وصف الزر لقارئ الشاشة.
- تُسجل القراءة عند ظهور الإشعار أثناء التمرير داخل النافذة.
- يجري التحديث كل 30 ثانية أثناء ظهور التطبيق، وعند العودة إليه أو فتح الإشعارات. تُحدّث عمليات الإدارة النافذة والعدّاد في الصفحة نفسها مباشرة.
- مسح بيانات المتصفح يعيد حالة القراءة. لا توجد مزامنة بين أجهزة مختلفة.

## التحقق المحلي

```text
npm.cmd run lint
npm.cmd run release
node scripts/test-notifications-ui.mjs
```

اختبار الواجهة يستخدم Chrome بملف مستخدم مؤقت وبيانات معزولة؛ يحجب جميع طلبات الخدمات الخارجية ولا يرسل بيانات إلى Supabase الفعلي. يمكن تحديد مسار Chrome بواسطة `CHROME_PATH`.

نسخة الويب الناتجة في `dist/index.html` و`index.html`. بناء Android وتطبيق SQL واختبار الصلاحيات على قاعدة البيانات الفعلية خطوات منفصلة.
