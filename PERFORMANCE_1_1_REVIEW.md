**وصال 1.1 — تقرير الفحص والإصلاح**

الإصدار السابق: **1.0**. الإصدار الظاهر للمستخدم: **1.1**. رقم Android ونسخة حزمة المشروع: **1.1.0**. رُفع `versionCode` مرة واحدة فقط من **4** إلى **5**.

شمل الفحص مسارات التطبيق، سياقات React والخطافات، استعلامات Supabase، البحث، السلايدرات، الصور والفيديو، الإشعارات والتفاعلات، لوحة الإدارة والوظائف، إعدادات Vite، وسكربتات النشر وAndroid. احتُفظ بالتعديلات الموجودة مسبقًا. لم تُنفّذ أي ملفات SQL ولم تتغير الجداول أو RLS أو صلاحيات الإدارة.

| السبب المثبت في المصدر | الإصلاح |
|---|---|
| `useCategories` ينشئ حالة وقراءة كاش وحسابات مستقلة في كل صفحة ونافذة رغم وجود كاش شبكة | `CategoriesProvider` واحد؛ نتيجة مشتركة بين المستهلكين، مع تحديث الذاكرة بعد تعديل الأقسام |
| جلب منفصل للأقسام في `useServices` لترجمة المعرفات | مشاركة قراءة الأقسام نفسها، ومدة صلاحية خمس دقائق، مع إبطالها بعد الكتابة |
| طلب سلايدر جارٍ غير مشترك؛ StrictMode يمكن أن يكرر الطلب | مشاركة الطلب الجاري، كاش خمس دقائق، تحديث الكاش بعد الحفظ، وإظهار الكاش المحلي أثناء التحديث |
| طلبات إشعارات متقاربة عند التركيب والتركيز والعودة للصفحة | مشاركة القراءة العامة وكاش عشر ثوانٍ؛ بيانات مسودات الإدارة لا تدخل الكاش العام |
| `Layout` يقرأ إحصاءات لا يعرضها، ويسجل الزيارة مرتين تحت StrictMode | تعطيل القراءة غير المستخدمة وحماية تسجيل الزيارة من إعادة تشغيل التأثير نفسه |
| طلب الخدمات كان يجلب حتى 40 صفًا بوسائط inline قبل أول عرض، ثم قد يطلب صفحة فارغة | جعل الدفعة الأولى 12 خدمة فقط، وتأجيل بقية الدفعات إلى وقت خمول المتصفح، وإنهاء التصفح الشبكي عند آخر دفعة فعلية؛ دمج طلبي خدمات المالك المعلقة والمرفوضة في طلب واحد |
| تحديث الخدمات يستبدل القائمة بالدفعة الأولى، وقد يحفظ قائمة ناقصة بعد خطأ صفحة لاحقة | دمج الدفعات مع البيانات السابقة؛ إزالة الصفوف القديمة فقط بعد اكتمال الجلب، مع منع نسخ مكررة للخدمة المحلية بعد اعتمادها |
| إعادة قراءة كاش الخدمات الكبير من IndexedDB قبل عرض الدفعة الأولى | إعادة استخدام نسخة الذاكرة التي قرأها التهيؤ بالفعل |
| فهرس البحث يُحسب داخل نافذة مركبة حتى عندما تكون مغلقة | تحميل النافذة وتركيبها عند فتحها فقط؛ تأخير حساب البحث 300ms بعد توقف الكتابة، مع إلغاء المؤقت السابق وإعادة استخدام فهرس المرادفات |
| كاش حساب دليل الأقسام يحتفظ بمفتاح واحد ويتبدل بين مستهلكي البيانات العامة والإدارية | كاش بمفاتيح ضعيفة لكل مصفوفة أقسام وخدمات، يسمح بتحرير اللقطات القديمة من الذاكرة |
| التصفح الاجتماعي يركب جميع المنشورات ويجلب تفاعلاتها دفعة واحدة | أول 12 منشورًا، ثم دفعات إضافية بزر «عرض المزيد»؛ التفاعلات للمنشورات المعروضة فقط |
| بناء مصفوفة بحجم عدد التفاعلات عند الضغط على إعجاب | حساب الملخص مباشرة من العدادات؛ تجاهل القراءات القديمة التي تسبق التفاعل أو تغير مجموعة المنشورات |
| ساعة الهيدر تعيد الرسم كل 100ms حتى أثناء عرض الشعار | تحديث كل ثانية أثناء ظهور الساعة فقط، مع التوقف في الصفحة المخفية |
| مؤقت السلايدر يستمر والصفحة مخفية | إيقاف مؤقتات السلايدر والتحميل المسبق في الخلفية وإعادتها عند ظهور الصفحة |
| صفحة تفاصيل الخدمة تكرر القراءة تحت StrictMode | مشاركة الطلب وكاش محدود لآخر 40 خدمة؛ الاحتفاظ بالخدمة الموجودة عند فشل تحديثها |
| بعض النوافذ الثقيلة تُستورد ضمن التحميل الأولي | فصل نوافذ الإضافة والبحث والإصدار والاقتراحات ودخول الإدارة وتحميلها عند الطلب |
| أمر `release` كان يدمج كل JavaScript وCSS، ما يلغي تقسيم الصفحات | `release` ينتج ملفات الويب المقسمة في `release/web`؛ التصدير المدمج أصبح `release:standalone` |
| البناء الأولي فشل لأن `JobSlidesAdmin.tsx` ينتهي بـ `__NEXT__` دون المكوّن المصدر | استكمال المكوّن باستخدام وظائف الجلب والحفظ والرفع والترتيب الموجودة بالفعل؛ لا ترقية SQL |

الصور العامة تستخدم `SafeImage` بتحميل كسول وفك ترميز غير متزامن، ويحمّل السلايدر الصورة التالية فقط. معاينة الفيديو الموجودة في نموذج الإضافة تستخدم `preload="metadata"`. لم تُغيّر الصور أو الفيديوهات المخزنة ولم تُنفّذ عملية ترحيل وسائط. قراءة الخدمات الحالية لا تزال تجلب جميع الصفوف على دفعات لتوفير البحث العام؛ إذا كانت الصفوف الفعلية تحتوي وسائط base64 كبيرة فإن حجمها يبقى عاملًا يحتاج قياسًا على البيانات المنشورة، ولا تعالج خاصية `loading="lazy"` حجم base64 الموجود أصلًا في JSON.

**التحقق**

- فحص TypeScript وبناء الإنتاج المقسّم.
- اختبارات منطق الكاش ومدة صلاحيته ومشاركة الطلبات، عدم استبدال البيانات المحفوظة باستجابة قديمة، دمج دفعات الخدمات، واختبارات البحث والتنقل والنماذج وتاريخ النشر وروابط التواصل والزيارات.
- اختبار التطبيق الفعلي في Chrome بوضع التطوير مع StrictMode وبوضع الإنتاج، مع اعتراض الاتصالات الخارجية واستبدال Supabase ببيانات مصطنعة. في سيناريو خمس خدمات: تسعة طلبات خلفية إجمالًا، منها طلب أقسام واحد وطلب سلايدر واحد وطلب خدمات معتمدة واحد؛ لا طلب صفحة خدمات إضافية فارغة.
- العودة للرئيسية داخل مدة الكاش لا تعيد جلب الأقسام أو السلايدر أو تفاعلات المنشورات. الكتابة لا تنشئ طلبات Supabase. تفاصيل الخدمة تشارك الطلب الجاري وتستخدم الكاش عند العودة.
- اختبار 85 خدمة مع تأخير وفشل الصفحة الثانية: البيانات السابقة تظل ظاهرة، ولا تُمسح من الكاش بسبب الفشل. عند اكتمال قراءة ناجحة تزال البيانات القديمة غير الموجودة في المصدر. زر «عرض المزيد» يزيد المنشورات من 12 إلى 24.
- اختبارات الإدارة المحلية نجحت: الدخول والتحقق من الدور، تغييرات التبويبات، تجميع أحداث Realtime، الموافقة والرفض والإضافة، أخطاء الحفظ، والاحتفاظ بالقوائم عند فشل التحديث.

نتائج المتصفح التفصيلية والصور: `release/performance-1.1/development-strict.json` و`release/performance-1.1/production.json`. نتائج الإدارة: `release/admin-performance-qa/results.json`. هذه اختبارات محلية وليست قياسات للموقع المنشور أو جهاز Android فعلي. لا توجد نسبة تحسن مقارنة بموقع الإنتاج؛ البناء الأصلي لم يكن يكتمل أصلًا. التحميل الأولي المحلي لملفات JavaScript وCSS يقارب **291 KB** من أجسام الاستجابات كما سجل المتصفح، دون الصور أو استجابات Supabase؛ والاختبار يتحقق من عدم تحميل النوافذ الثقيلة المذكورة عند البداية.

**التسليم والتشغيل**

`npm run release` يجهز `release/web`. ارفع محتويات المجلد كاملة إلى الاستضافة، بما فيها `assets`؛ لا ترفع الإندكس وحده. GitHub Actions الحالي يرفع `dist` المقسّم أصلًا. الرقم داخل واجهة التطبيق مستورد من `src/lib/appVersion.ts`.

تشغيل اختبارات المنطق: `npm run test:performance`. اختبار المتصفح: `node scripts/test-performance-browser.mjs http://localhost:3001 development-strict` بعد تشغيل خادم التطوير، أو عنوان `4173` مع `production` بعد تشغيل المعاينة. جميع طلبات Supabase في هذا الاختبار مصطنعة، بما فيها عمليات الزيارات.

لم يُنشر التحديث إلى موقع خارجي أو GitHub أو متجر التطبيقات. إعدادات Android تخص 1.1.0؛ بناء APK/AAB وتثبيته على جهاز فعلي خطوة منفصلة عن اختبار الويب.
