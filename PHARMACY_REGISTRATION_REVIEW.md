# نموذج انضمام الصيدليات — تقرير ما قبل تغيير قاعدة البيانات

## حالة التنفيذ

تم تجهيز معاينة كاملة داخل `AddServiceModal` نفسه، اعتمادًا على إعداد `fields.registration` المركزي للصيدليات. لا يوجد نموذج منفصل للصيدليات ولا نسخ مستقلة لكل صفحة.

رابط المعاينة عند تشغيل Vite على المنفذ 3002:
`http://localhost:3002/.cache/pharmacy-registration-preview.html`

هذه معاينة صريحة: زر «اختبار النموذج» يتحقق من الحقول ويعرض ملخصًا محليًا. لا يستدعي `addService` ولا يرفع الملفات. نموذج الانضمام العامل يبقى على سلوك الحفظ الحالي إلى حين اعتماد تخزين الحقول الجديدة.

## ما تم التحقق منه

- الاستعلام القرائي `services?select=*&status=eq.approved&limit=1` نجح، واستُخدم لفحص أسماء الأعمدة فقط.
- توجد `title`, `description`, `phone`, `address`, `image_url`, `video_url`, `experience`, `certificates`, `bio`, `category_id`, `status`.
- لا توجد أعمدة لمعرض الصور أو اسم الصيدلي أو المحافظة أو سنوات الخبرة بشكل مستقل.
- `experience`, `certificates`, `bio` كانت فارغة في الصف المقروء؛ لا يمكن إثبات أنواعها من هذه العينة. مخطط OpenAPI الكامل يحتاج صلاحية غير متاحة للمفتاح العام، ولم تُطلب أو تُستخدم مفاتيح إدارية.
- مسار الحفظ الحالي في `useServices.ts` يرسل صورة واحدة إلى `image_url`، والمميزات إلى `description`، والعنوان إلى `address`، والفيديو إلى `video_url`.
- لا يوجد مسار تطبيق جاهز لرفع ملفات ممارسة المهنة وتقديمها للمراجع بصلاحية خاصة.

## الحقول في المعاينة

| الحقل | التحقق / التخزين المقترح |
|---|---|
| اسم الصيدلية | مطلوب؛ `title` الموجود |
| اسم صاحب الصيدلية / الصيدلي | مطلوب؛ داخل `registration_details` المقترح |
| شهادة أو كتاب ممارسة المهنة | وصف/رقم الكتاب والجهة مطلوب؛ بيانات مراجعة خاصة مقترحة |
| مرفق الممارسة | اختياري؛ صورة JPG/PNG/WebP أو PDF حتى 5 MiB، منفصل عن الصور العامة |
| رقم الهاتف | مطلوب؛ `phone` الموجود |
| المحافظة | مطلوبة؛ داخل `registration_details` المقترح |
| المنطقة | مطلوبة؛ `address` الموجود |
| سنوات الخبرة | عدد صحيح غير سالب ومطلوب؛ داخل `registration_details` المقترح |
| المميزات والخدمات | مطلوبة؛ `description` الموجود |
| صور الصيدلية | 1–5 صور سليمة JPG/PNG/WebP، حتى 2 MiB للصورة؛ الأولى غلاف |
| الفيديو | اختياري؛ نفس معالجة الفيديو الحالية: ثانية واحدة إلى 30 ثانية مع هامش قياس 0.05 ثانية |

## التغيير المطلوب لاعتماد الحفظ الكامل

1. إضافة `registration_details jsonb` إلى `services` لحفظ اسم الصيدلي والمحافظة وسنوات الخبرة بشكل منظم وقابل لإعادة الاستخدام في إعدادات الأقسام. لا تُخزّن هذه البيانات خفية داخل نص الوصف.
2. إضافة `gallery_images text[]` لحفظ مسارات 1–5 صور، مع الإبقاء على `image_url` كغلاف للتوافق مع البطاقات الحالية. تهيئة رفع الصور وعرض المعرض وقراءة الحقول الجديدة وتعديلها دون فقد البيانات القديمة.
3. فصل وصف مستند الممارسة وملفه في سجل تحقق خاص مرتبط بمعرّف الخدمة، مع تخزين الملف في مساحة خاصة ومسار مراجعة يتيح للإدارة الحالية قراءته. لا يُوضع المستند داخل صور الصيدلية أو عمود عام قابل للقراءة بعد اعتماد الخدمة.
4. اعتماد سياسات الوصول الخاصة بالمرفقات الجديدة بعد مراجعة صلاحيات الإدارة الحالية. لا تُغيَّر قواعد اعتماد الخدمات الحالية أو معرّفات الأقسام أو slugs.
5. تظل الخدمة الجديدة `pending`، وتستخدم قراري الموافقة والرفض الحاليين. لا تُنشر قبل الموافقة. يجب إظهار الحقول والمرفقات الجديدة للمراجع قبل تفعيل النموذج للعملاء.

لا توجد ترحيلات SQL أو تغييرات قاعدة بيانات أو Storage أو RLS منفّذة في هذه المرحلة. هذه البنود تنتظر موافقة المستخدم وفق شرطه السابق: «إذا احتاجت بعض الحقول تغييرًا في قاعدة البيانات، أعطني تقريرًا أولًا قبل التنفيذ».
